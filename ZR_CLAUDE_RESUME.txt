Authority: goal.md; resume $goal ./goal.md.
Profile: fevm-gdpr2, explicitly selected. User waived quota/expiry prerequisites.
Classic is confirmed unsupported; do not retry or substitute profiles. ZR-9 stays parked.
Public/synthetic data only. Preserve .DS_Store and .isaac. No push requested.

ZR-3 iterations 1/2/3 measured retrieval, classifier and compact features. Selected Levenshtein/GBT and IDF-only token features; shipped token defaults still need reconciliation.
Linkage iteration 4 failed before fitting, in QueryExecution.explainString with Java heap exhaustion.
Iteration 5 uses owned table boundaries on weighted inputs, candidates and vectors with unchanged budgets.
Iteration 5 completed all three FEBRL variants. Current sweep: tools/candidate_pairs_sweep.py, session 44448 (ZR-3 iteration 6).
Do not start another Spark experiment until it is terminal.

ZR-4 iteration 5 completed native comparisons on both corpora. Verified merge wins: macro pairwise F1 .9696, B-cubed .9727.
Iteration 6 identity checks passed on FEBRL3 and historical_50k; reports independently replay journals and cluster events.
See bench/CLUSTERS.md and bench/IDENTITY.md. No confirmation scoring or promotion.
Normal cluster CLI and atomic local publication passed iteration 7: five fresh calls, three commits; crosswalks and journals match sealed FEBRL3 evidence. Three recovery tests include abrupt process death. tools/verify_clusters.py audits this evidence. Later save-scores CLI/engine additions require final compatible-source refresh.
Cluster default max_rounds still 20, while historical verified merge needed 21; benchmark uses 30.
Remote publication/SDP/DQX, APX, Genie, scale, latency and confirmation remain pending.
APX maintenance remains unconfirmed; no framework substitution is authorized. Baseline scaffold built (169758 total bytes; max file169627), stored in app/ with upstream license/notice. No custom UI/deployment.
Affected ZR-1/2/5 evidence needs refresh on coherent final source. Latest full suites predate new changes.

Prepared but not run: tools/freeze_benchmarks.py and run_frozen_linkage.py. Freeze only after candidate selection; no confirmation has been scored. Final scoring uses normal CLI with --save-scores, complete universe, full startup/output/cleanup timing.
