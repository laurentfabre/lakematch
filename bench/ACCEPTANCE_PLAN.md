# Final local integration acceptance — ZR-3 and ZR-4 iteration 8

This is the last authorized experiment iteration for each phase. Begin only
after active serverless work is terminal. A remaining red gate is a diagnosis
to surface, not permission for a ninth iteration. Do not retry the million-row
heap failure or raise memory, candidate, join or time limits.

Hypothesis: adopting the already selected validation defaults and adding CLI
orchestration preserves the eight frozen models' predictions and completes the
local integration checks. Select MinHash, Levenshtein, IDF token cosine and GBT;
keep verified merge and raise its shipped round bound from 20 to the already
measured 30-round envelope. These are validation-derived configuration choices,
not tuning to confirmation outcomes. Existing benchmark/tracking fixtures must
explicitly retain their historical methods and feature sets.

## Frozen evidence compatibility

The original `bench/freeze.json` remains byte-for-byte unchanged (SHA-256
`d703ebc6f22bc1ee1534d6bfed3c1761f931df16eb0875c9e3501fdd84ea8829`).
The source archive indexed by `frozen_sources.json` remains the original source.
Before replay, record exact current source hashes and the reviewed differences
in a separate compatibility declaration. Reject unlisted changes. The current
changes dispatch native quality through a lazy adapter and remove a global
integration guard from the feature stage; native row rules and expressions are
unchanged. Full frozen configs explicitly record the settings whose defaults
will change. Benchmark CLI dispatch and the compatibility guard are additions
to orchestration, not model changes.

Replay the two full-universe FEBRL runs through fresh normal CLI processes and
all six supplied-pair held-out runs using the same eight immutable models,
corpora, seeds, thresholds and full configs. Compare complete candidate-pair
identities, cosine metadata and probabilities with sealed iteration-7 outputs;
require identical decisions and probabilities within 1e-12. This is an execution
compatibility replay of already exposed outcomes, not a new confirmation set.
Any changed decision fails compatibility; do not tune or re-freeze to repair it.
Keep the original evidence and the replay separately in the append-only ledger.

Run the exposed original 5,000-by-5,000 FEBRL diagnostic with the frozen
all-field model and a separately measured nearest-neighbour-only gram baseline.
Report both scores and startup-inclusive times without asserting that the
original-corpus nearest-neighbour result generalizes to half-unmatched data.
Expose `lakematch bench --all` as a finite sequential campaign entry point;
distinguish execution from read-only evidence aggregation and preserve failures.
Do not use a cached report as proof that this CLI executed every corpus.
The campaign entry point must stop before the scale stage when the ledger
contains the retained failed million-record run. Record a blocked stage and a
nonzero exit; the already measured ladder is historical evidence, not fresh
CLI execution. This guard does not authorize a repeat or satisfy the full
`bench --all` acceptance gate.

## Clustering and affected gates

Repeat the two native clustering comparisons, both exact mutation checks and
the normal local cluster CLI publication/recovery check with the final source.
Use `cluster_sweep.py native --estimator gbt --replay`: the original two
validation models, IDF artifacts and thresholds are hash-bound in
`cluster_replay_reference.json`. Compare every scored edge and membership
with the original sealed outputs before accepting the replay. Do not refit.
Reuse the unchanged isolated Splink baseline only if its execution dependencies
still match. Compare the resulting memberships and journals with the preserved
prior validation measurements. No new models, methods, seeds or budgets.

Refresh the affected package, full classic and Connect suites, offline CLI,
feature ablations and MLflow checks on coherent source. Their own phase iteration
counts and bounds apply. No remote operations overlap another experiment.

## Bounds and reporting

One Spark process group at a time, unchanged local[2] and default JVM memory;
external network denied for local engine experiments. Frozen replay: 2 x 300s
plus 6 x 420s; original diagnostic: 600s. Clustering: 2 x 900s comparisons,
2 x 600s identity runs and 600s CLI, plus 120s pure recovery checks. Total ZR-3/4
timeout envelope is 7,440s plus owned process cleanup. Stop the dependent sweep
at its first execution or semantic-parity failure and retain the diagnosis.

The million-row measured failure, Affiliations reference comparability limits,
unreconciled billing and any missing CLI execution evidence remain visible.
Implementing a runner does not turn those gates green. No push or release.
